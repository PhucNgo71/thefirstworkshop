# thefirstworkshop retail

Bilingual retail storefront prototype for thefirstworkshop, including product discovery, category filtering, interactive product hotspots, product configuration, cart, and demo checkout.

## Deployment

The production-ready static site is located in `dist/`. Vercel is configured through `vercel.json` to publish that directory directly.

The checkout is a learning demonstration and does not collect real payments.
